class Api {
    /*
    findAreaStats(uuid: string, canceler?: HttpCanceler): AxiosPromise<ApiCity> {
        let config: AxiosRequestConfig = {};
        if (canceler) {
            config.cancelToken = new axios.CancelToken(canceler);
        }

        return axios.get<ApiCity>('/api/cities/'+uuid, config);
    }
     */
}
